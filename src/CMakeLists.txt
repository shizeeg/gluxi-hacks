#include(${QT_USE_FILE})

INCLUDE (plugins)

INCLUDE_DIRECTORIES(
	${INCLUDE_DIRECTORIES}
	${CMAKE_CURRENT_SOURCE_DIR}
)

GLUXI_FIND_PLUGINS(GLUXI_PLUGINS ${CMAKE_CURRENT_SOURCE_DIR}/plugins)

ADD_SUBDIRECTORY(plugins)
ADD_SUBDIRECTORY(base)
ADD_SUBDIRECTORY(socketw)

